# 🟩 **4. v6.0-rc Supervisor Activation Checklist**

File: `docs/v6.0-rc_supervisor_activation_checklist.md`

---

# **Supervisor Activation Checklist — v6.0-rc**

### **Step 1 — Version & Code**

* [ ] VERSION contains `v6.0-rc`
* [ ] Latest main branch pulled
* [ ] No v5 supervisor configs present

### **Step 2 — Flags**

Set:

```
INTEL_V6_ENABLED=1
RISK_ENGINE_V6_ENABLED=1
PIPELINE_V6_SHADOW_ENABLED=1
FEEDBACK_ALLOCATOR_V6_ENABLED=1
```

(Optional Enable):

```
ROUTER_AUTOTUNE_V6_ENABLED=1
ROUTER_AUTOTUNE_V6_APPLY_ENABLED=1
ROUTER_AUTOTUNE_V6_SYMBOL_ALLOWLIST="BTCUSDT,ETHUSDT,SOLUSDT"
```

### **Step 3 — Supervisor Start**

```
sudo supervisorctl restart hedge:
sudo supervisorctl status hedge:
```

Expect 5 services running with no crash loop.

### **Step 4 — Runtime Probe**

Check:

* [ ] `logs/state/v6_runtime_probe.json` has correct engine_version
* [ ] v6 flags all present

### **Step 5 — State/Telemetry**

* [ ] `synced_state.json` present with updated_at
* [ ] `nav.json` present
* [ ] `positions.json` present
* [ ] router_health present

### **Step 6 — Pipeline**

* [ ] `pipeline_v6_shadow_head.json` updating
* [ ] `pipeline_v6_compare_summary.json` exists
* [ ] `is_warmup` expected (true if <200 samples)

### **Step 7 — Router Auto-Apply**

If enabled:

* [ ] router_policy_suggestions present
* [ ] No repeated “missing allocator” warnings
* [ ] allocator file fresh (timestamp valid)
* [ ] apply logs observed

### **Step 8 — Firestore**

If enabled:

* [ ] `ALLOW_PROD_SYNC=1`
* [ ] NAV and positions published

### **Step 9 — Tests**

Run:

```
pytest tests/test_v6_runtime_activation.py
pytest tests/test_executor_state_files.py
pytest tests/test_pipeline_v6_compare_runtime.py
pytest tests/test_router_autotune_apply_v6.py
```

* [ ] All tests pass

### **Step 10 — Ready for Stage**

Once all boxes ticked → Tag: `git tag v6.0-rc`.
