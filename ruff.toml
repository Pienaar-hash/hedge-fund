# Keep defaults otherwise; this config narrows scope to stable, lintable code.

extend-exclude = [
  "dashboard/",
  "scripts/",
  "research/",
  "ops/",
  "deploy/",
  "archive/",
  "prediction/",
  "treasury/",
  "tests/legacy/",
  "execution/flatten_all.py",
  "execution/*bak*",
]

[lint]
exclude = [
  "dashboard/",
  "scripts/",
  "research/",
  "ops/",
  "deploy/",
  "archive/",
  "prediction/",
  "treasury/",
  "tests/legacy/",
  "execution/flatten_all.py",
  "execution/*bak*",
]

[lint.per-file-ignores]
"dashboard/dashboard_utils.py" = ["E402"]
